<!doctype html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.rtl.min.css" integrity="sha384-OXTEbYDqaX2ZY/BOaZV/yFGChYHtrXH2nyXJ372n2Y8abBhrqacCEe+3qhSHtLjy" crossorigin="anonymous">
    <script src="https://unpkg.com/boxicons@2.1.2/dist/boxicons.js"></script>
    <title>EDITAR BARRACO</title>
    <style>
      body{
        background-color: rgb(177, 195, 214);
      }
    </style>
  </head>
  <body>
 
    <center style="padding-top: 10px;"><h1>EDITAR BARRACO</h1> </center>

   <% if (user){ %>
      <div class="container mt-4">
        <div class="card border-secondary" style="background-color: rgba(154, 149, 182, 0.578);">
            <div class="card-header">Editar Registro</div>
            <div class="card-body">
                <form action="/update2" method="POST">
                    <input id="id" name="id" value="<%= user.id %>" hidden>

                    <div class="mb-3">
                      <label for="user" class="form-label">ARETE</label>
                      <input type="text" class="form-control" id="arete" name="arete" value="<%=user.arete %>" tabindex="1">
                  </div>

                    <div class="mb-3">
                        <label for="user" class="form-label">FECHA INGRESO</label>
                        <input type="text" class="form-control" id="fechaIn" name="fechaIn" value="<%=user.fechaIn %>" tabindex="1">
                    </div>

                    <div class="mb-3">
                        <label for="user" class="form-label">NOMBRE</label>
                        <input type="text" class="form-control" id="nombre" name="nombre" value="<%=user.nombre %>" tabindex="1"> <!-- el nombre del label es user y el ID y el NAME del type formarn el vinculo por ejemplo  user.name-->
                      </div>
                    <div class="mb-3">
                        <label for="user" class="form-label">EDAD</label>
                        <input type="text" class="form-control" id="edad" name="edad" value="<%=user.edad %>" tabindex="1"> <!-- el nombre del label es user y el ID y el NAME del type formarn el vinculo por ejemplo  user.name-->
                      </div>
                    <div class="mb-3">
                        <label for="user" class="form-label">PESO LBS</label>
                        <input type="text" class="form-control" id="peso" name="peso" value="<%=user.peso %>" tabindex="1"> <!-- el nombre del label es user y el ID y el NAME del type formarn el vinculo por ejemplo  user.name-->
                      </div>
                     <div class="mb-3">
                        <label for="user" class="form-label">FECHA DE VACUNACIÓN</label>
                        <input type="text" class="form-control" id="fvacunacion" name="fvacunacion" value="<%=user.fvacunacion %>" tabindex="1"> <!-- el nombre del label es user y el ID y el NAME del type formarn el vinculo por ejemplo  user.name-->
                      </div>
                      <div class="mb-3">
                        <label for="user" class="form-label">FECHA DE MONTA</label>
                        <input type="text" class="form-control" id="fmonta" name="fmonta" value="<%=user.fmonta %>" tabindex="1"> <!-- el nombre del label es user y el ID y el NAME del type formarn el vinculo por ejemplo  user.name-->
                      </div>
                      <div class="mb-3">
                        <label for="user" class="form-label">CANTIDAD DE MONTAS</label>
                        <input type="text" class="form-control" id="cantmontas" name="cantmontas" value="<%=user.cantmontas %>" tabindex="1"> <!-- el nombre del label es user y el ID y el NAME del type formarn el vinculo por ejemplo  user.name-->
                      </div>
                    <button type="submit" class="btn btn-primary" tabindex="5">Guardar</button>
                    <a href="/menu_ingreso_barracos" class="btn btn-secondary">Cancelar</a>
                </form>
            </div>
        </div>
      </div>
      <% } %>
  </body>
</html>